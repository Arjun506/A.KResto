# UI

## Frontend Framework

- Next.js App Router
- React
- Tailwind CSS

## UX Features (discovered)

- Role-based dashboard routing
- Dark mode support (ThemeProvider in layout)
- Recharts visualizations on billing dashboard

## Auth UI

- `apps/web/app/login/page.tsx`
  - Portal toggle: restaurant vs super-admin
  - Staff role selector
  - Calls backend `loginUser()` with fallback JWT bypass for password `654321`

## Dashboard UI

- `apps/web/app/dashboard/billing/page.tsx`: mock KPIs + charts + recent orders table
- `apps/web/app/dashboard/kitchen/page.tsx`: rich KDS multi-tab UI (mock)
- `apps/web/app/dashboard/waiter/page.tsx`: waiter desk multi-tab UI (mock)
- `apps/web/app/dashboard/page.tsx`: switches to role components (dynamic imports)

## TODO

- Enumerate reusable UI components under `apps/web/components/common/*` and `apps/web/components/dashboard/*`.
