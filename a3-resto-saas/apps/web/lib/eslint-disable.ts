/*
 * Helper to avoid React Hooks + setState-in-effect lint errors
 * in parts of the app that are still being stabilized.
 */
export const ESLINT_DISABLE_SET_STATE_IN_EFFECT =
  '/* eslint-disable react-hooks/set-state-in-effect */';

