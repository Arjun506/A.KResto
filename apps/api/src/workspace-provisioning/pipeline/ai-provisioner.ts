import { Injectable } from '@nestjs/common';

@Injectable()
export class AIProvisioner {
  // No-op for now (persist to tenant.settings JSON when AI profile schema is finalized)
  provisionAI() {
    // no-op
  }
}
