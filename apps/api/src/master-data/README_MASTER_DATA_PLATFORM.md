# Master Data Platform (Sprint 6)

Foundation services for centralized master data shared across business domains.

This sprint creates:

- Prisma models (multi-tenant)
- NestJS module exposing generic CRUD endpoints per resource type
- Permission keys per resource

No business-specific logic (Products/Inventory/Restaurant) is implemented here.
