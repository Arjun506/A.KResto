# Phase 31 Wave 6 — Super Admin Control Plane

---

## Central Platform Administration

- **Tenant Lifecycle Control**: Support for `PROVISIONING`, `ACTIVE`, `SUSPENDED`, `CLOSED` tenant states.
- **Pilot Management**: Pilot control center endpoints for onboarding and monitoring pilot business tenants (`apps/api/src/super-admin/`).
- **Feature Flags & Overrides**: Server-side feature flag evaluation per tenant.
